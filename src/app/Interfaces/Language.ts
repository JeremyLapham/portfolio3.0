import { ReactNode } from "react";

export interface IIntroIcons {
    icon: ReactNode;
    link: string;
    target: string;
    show: boolean
}

export interface LanguageIcon {
    icon: ReactNode;
    text: string;
}
